const a  = ()=>{
    return new Promise((resolve,reject)=>{
        setTimeout(()=>{
            resolve(text)
        },2000)

    })
}
{
    function sum(a,b,c){
        return a+b+c
    }
    let x = [1,2,5]
    console.log(sum(...x))
    async ()=>{

        let text = await a(" i am resoiving in after 1  second")
        console.log(text)
        text = await a("I am resoving in after 2 second")
        console.log(text)
    }
    }
function simpleInteresr(p,q,r){
    return(p*q*r)/100

}
console.log(simpleInteresr(1000,5,1))
